# Runtime dependencies for freeflo (macOS).
# Install into a virtualenv:  pip install -r requirements.txt

rumps>=0.4.0                        # macOS menu-bar app framework
sounddevice>=0.5.0                  # microphone capture (bundles PortAudio + cffi)
numpy>=2.0                          # audio buffer handling
scipy>=1.13                         # WAV file writing
pyperclip>=1.9                      # clipboard-based text injection

# Optional Google Drive backup (engine/gauth.py, engine/backup.py)
google-auth>=2.35.0
google-auth-oauthlib>=1.2.0         # OAuth loopback (RFC 8252) sign-in flow
requests>=2.32.0                    # plain REST calls to the Drive API
keyring>=25.0.0                     # refresh token storage in the macOS Keychain
# google.auth.crypt imports these, but recent google-auth no longer pins
# cachetools/rsa, so declare them explicitly to keep clean installs working.
cryptography>=42.0
cachetools>=5.0
rsa>=4.9

# Opt-in, consent-gated telemetry (engine/telemetry.py) — inert without keys
posthog>=3.0,<4                     # anonymous usage analytics + identity
sentry-sdk>=2.0                     # crash reporting

# PyObjC bridges (pull in pyobjc-core automatically)
pyobjc-framework-Cocoa>=10.0
pyobjc-framework-Quartz>=10.0       # CGEventTap hotkey listener
pyobjc-framework-WebKit>=10.0       # history/settings window
pyobjc-framework-ApplicationServices>=10.0  # Accessibility permission check
pyobjc-framework-AVFoundation>=10.0  # microphone authorization (onboarding)

# Building a standalone .app bundle (optional — see README "Building")
# py2app>=0.28
