<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Hardened-runtime entitlements for the notarized freeflo build.

  A py2app/Python bundle loads its own interpreter and several bundled,
  non-Apple dylibs (portaudio, whisper-cli, compiled crypto/cffi extensions),
  so library validation must be relaxed and the interpreter needs JIT/writable-
  executable memory. The microphone entitlement pairs with
  NSMicrophoneUsageDescription in Info.plist; Apple-events covers the
  clipboard-paste AppleScript in engine/injector.py.
-->
<plist version="1.0">
<dict>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
    <key>com.apple.security.device.audio-input</key>
    <true/>
    <key>com.apple.security.automation.apple-events</key>
    <true/>
</dict>
</plist>
