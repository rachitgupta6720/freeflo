# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
*.egg

# Virtual environments
.venv/
venv/
env/

# py2app / build output
build/
dist/

# Bundled whisper.cpp binary & GGML models (users provide their own — see README)
whisper-cli
*.bin

# Google OAuth client baked into the build (holds the client secret) — never commit
google_client.json

# macOS
.DS_Store

# Editors
.idea/
.vscode/
*.swp
